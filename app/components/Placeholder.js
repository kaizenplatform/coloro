'use strict';

var React = require('react');

var Placeholder = React.createClass({
  render: function () {
    return (
      <div className="placeholder">Copy color hex code or color name</div>
    )
  }
});

module.exports = Placeholder;
